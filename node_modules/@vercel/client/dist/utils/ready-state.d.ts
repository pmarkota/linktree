import { Deployment, DeploymentBuild } from '../types';
export declare const isReady: ({ readyState, state, }: Deployment | DeploymentBuild) => boolean;
export declare const isFailed: ({ readyState, state, }: Deployment | DeploymentBuild) => boolean;
export declare const isDone: (buildOrDeployment: Deployment | DeploymentBuild) => boolean;
export declare const isAliasAssigned: (deployment: Deployment) => boolean;
export declare const isAliasError: (deployment: Deployment) => boolean;
